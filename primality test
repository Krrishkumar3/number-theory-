#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
	if(n == 1){
	    cout << "not a prime";
	}
	int count = 0;
	for(int i=2;i*i<=n;i++){
	    if(n%i==0){
	        count++;
	    }
	}
	if(count >= 1){
	    cout << "it is a prime";
	}
	else{
	    cout << "not a prime";
	}
}
